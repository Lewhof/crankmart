/**
 * Universal test serial seeded by scripts/seed-test-users.ts (stolen_reports table)
 * with status='approved'. Used by /sell publish-gate tests to confirm the stolen
 * registry blocks listings of known-stolen frames.
 *
 * Reporter is reg_sa; serial is country-agnostic.
 */
export const STOLEN_TEST_SERIAL = 'TEST-STOLEN-001'
